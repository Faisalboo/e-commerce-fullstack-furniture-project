export type Product = {
  id: string
  name: string
  image: string
  description: string
  createdAt: string
  price: number
  categoryId: string
}
export type Category = {
  id: string
  name: string
}
